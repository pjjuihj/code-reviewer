# 代码审查专家（C / 嵌入式 / STM32 / FreeRTOS）

> Claude Code Skill — 专业的嵌入式代码审查，覆盖 14 个维度、200+ 检查项

## 功能特性

### 14 个审查维度

| # | 维度 | 检查项数 | 说明 |
|---|------|---------|------|
| 1 | C 语言安全 | 25 | 指针、内存、类型、位域、对齐 |
| 2 | 嵌入式专项 | 28 | 中断、栈、寄存器、看门狗、电源、启动 |
| 3 | STM32/HAL | 30 | HAL 库、CubeMX 一致性、8 种外设 |
| 4 | FreeRTOS | 17 | 任务、同步、队列、内存管理 |
| 5 | 通用最佳实践 | 7 | 命名、错误处理、模块边界 |
| 6 | 代码复杂度 | 12 | 函数/文件/项目级度量 |
| 7 | 固件安全 | 16 | Flash 保护、完整性、通信安全 |
| 8 | 并发与实时性 | 16 | 竞态、优先级、死锁、实时性 |
| 9 | 构建系统 | 16 | 编译器、链接脚本、预处理器 |
| 10 | 代码示例 | 14 组 | ❌ 错误 → ✅ 修正 对照 |
| 11 | 可测试性 | 13 | 测试设计、Mock、覆盖率 |
| 12 | 静态分析工具 | 12 | PC-lint、Cppcheck 集成 |
| 13 | MISRA-C 合规 | 17 | 强制/建议规则、合规等级 |
| 14 | 审查工作流 | — | 三阶段审查法 |

### 快速检查清单（10 秒扫描）

```
□ 所有 HAL_xxx() 返回值是否检查？
□ ISR 共享变量是否 volatile？
□ sprintf/strcpy 是否有长度限制？
□ 时钟配置是否被代码修改？
□ 栈上数组是否 > 256 字节？
□ FreeRTOS 中是否用了 HAL_Delay？
□ ISR 中是否有 printf/Delay/浮点？
□ switch 是否有 default 分支？
□ DMA 缓冲区是否对齐？
□ 中断标志是否在 ISR 中清除？
```

### 三阶段审查法

```
阶段 1: 预审（5 分钟）
  ├─ 快速扫描清单
  ├─ 确认代码能编译通过
  └─ 判断是否需要深入审查

阶段 2: 深度审查（15-30 分钟）
  ├─ 按 14 个维度逐项检查
  ├─ 重点关注 Critical/High
  └─ 检查边界条件和错误路径

阶段 3: 后审（5 分钟）
  ├─ 汇总问题，按严重度排序
  ├─ 提供具体修复代码
  └─ 记录审查结论
```

## 使用方法

### 作为 Claude Code Skill

将 `skill.md` 放入 Claude Code skills 目录，使用 `/code-reviewer` 调用。

### 直接使用

1. 将 `skill.md` 内容作为系统提示词
2. 发送需要审查的代码
3. 按 14 个维度获得结构化审查报告

## 输出示例

```
## 代码审查报告：main.c

### 统计
- Critical: 1 | High: 2 | Medium: 3 | Low: 1
- 代码质量评分: 5/10
- 审查维度: C语言安全、STM32/HAL、中断安全

### 快速扫描
✗ HAL_xxx() 返回值未检查 (3处)
✓ ISR 共享变量已 volatile
✗ sprintf 无长度限制 (1处)

### 问题列表

#### [Critical] 空指针解引用
- **位置**: main.c:42
- **问题**: HAL_UART_Receive 返回值未检查，直接使用缓冲区
- **风险**: 接收失败时访问未初始化数据，导致 HardFault
- **修复**:
  ```c
  if (HAL_UART_Receive(&huart1, buf, len, 100) != HAL_OK) {
      Error_Handler();
  }
  ```

### 改进优先级
1. 修复空指针解引用（Critical）
2. 检查 HAL 返回值（High）
3. 替换 sprintf 为 snprintf（Medium）

### 总结
代码存在 1 个 Critical 问题，需立即修复。整体结构清晰，但错误处理不足。
```

## 严重等级

| 等级 | 定义 | 示例 |
|------|------|------|
| **Critical** | 必然导致崩溃/死机/数据损坏 | 空指针解引用、栈溢出、时钟被改 |
| **High** | 可能导致崩溃或安全漏洞 | 缓冲区溢出、ISR 耗时、竞态条件 |
| **Medium** | 影响可靠性或可维护性 | 未检查返回值、魔法数字 |
| **Low** | 代码风格或小改进 | 命名不规范、缺少注释 |

## 评分标准

| 分数 | 含义 |
|------|------|
| 9-10 | 生产级，无 Critical/High，可直接发布 |
| 7-8 | 良好，有少量 Medium，建议修复后发布 |
| 5-6 | 可用，有 High 问题需修复后发布 |
| 3-4 | 较差，有 Critical 问题，需重写关键部分 |
| 1-2 | 严重缺陷，存在多个 Critical，需全面重写 |

## 适用场景

- STM32 固件开发代码审查
- 嵌入式 C 代码安全审计
- FreeRTOS 应用代码检查
- CubeMX 配置一致性验证
- MISRA-C 合规性检查
- PR 代码审查

## 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| v4.0 | 2026-06 | 14 维度 + 代码示例 + 审查工作流 |
| v3.0 | 2026-06 | 新增并发/固件安全/构建系统 |
| v2.0 | 2026-06 | 新增外设专项/FreeRTOS/复杂度 |
| v1.0 | 2026-06 | 初始版本 |

## License

MIT
